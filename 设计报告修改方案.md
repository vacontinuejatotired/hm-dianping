# 设计报告内容（经审核版）

## 修改清单

以下列出相对于当前 docx 需要修改/补充的部分：

---

### 修改 1：封面页 → 指导教师
- **位置**：封面页第22段
- **改动**：将"指导教师      xx" → "指导教师      包博文"

### 修改 2：页眉
- **位置**：正文页眉（Section 3）
- **改动**：将"基于Web的进销存管理系统开发" → "基于Spring Boot的小众点评系统开发"

### 修改 3：插入插图
将所有【插图位置：...】占位符替换为对应的 PNG 图片。图片文件已就绪：
- 系统总体架构图.drawio.png → 图3-1
- 图3-2_双令牌鉴权流程.drawio.png → 图3-2
- 图3-3_多级缓存读取流程.drawio.png → 图3-3
- 图3-4_附近商户搜索.drawio.png → 图3-4
- 图3-5_社交互动功能.drawio.png → 图3-5

### 修改 4：2.5 社交互动技术 → 补充完整内容
当前 docx 中该节内容过于简略，需要补充：
- 探店笔记：发布流程（先存草稿→上传图片→updateBlogImages）
- 点赞功能：Redis ZSet 存储（用户ID+时间戳），Redis Set 去重，点赞排行榜
- Feed流：推模式（笔记发布时推送到粉丝ZSet时间线），滚动分页（rangeByScoreWithScores）

需要补充的段落文本：
"社交互动模块主要包括探店笔记、点赞功能和关注Feed流三大功能。探店笔记采用先存草稿后上传图片的发布流程：saveBlog()生成笔记草稿后，由updateBlogImages()上传图片并调用pushBloToFansBatch()将笔记ID推送到所有粉丝的Feed流ZSet中。点赞功能基于Redis的ZSet存储点赞用户ID和时间戳，同时使用Set做用户维度的去重校验，支持查询点赞排行榜Top5。Feed流基于推模式设计，笔记发布时批量插入所有粉丝的ZSet时间线（采用Redis Pipeline优化），查询时使用rangeByScoreWithScores实现滚动分页，解决传统分页在ZSet中数据偏移的问题。关注关系通过MySQL的follow表维护。"

### 修改 5：3.1 系统分析与总体设计 → 补充完整描述
"系统采用前后端分离架构，后端基于Spring Boot 3.x框架，前端基于Vue 3 + Element Plus。系统核心功能模块包括用户认证、商铺缓存、位置服务和社交互动四大模块。

（1）用户认证模块：基于双令牌机制（Access Token + Refresh Token），结合JWT、Caffeine、Redis和Lua脚本，实现登录态的签发、校验和自动续期。
（2）商铺缓存模块：多级缓存架构（Caffeine→Redis→MySQL），采用逻辑过期方案处理热点数据，配合空值缓存和互斥锁防范缓存穿透与击穿。
（3）位置服务模块：利用Redis GEO数据类型管理商铺位置信息，支持按半径搜索附近商户，配合MySQL回查详情。
（4）社交互动模块：基于Redis ZSet实现点赞排名和关注者Feed流，采用推模式推送笔记动态。"

### 修改 6：3.1.4 社交互动模块设计 → 补充完整内容
当前内容已存在，但需要补充更多代码细节描述。需要追加的段落文本：
"探店笔记的发布流程为两步：先调用saveBlog()保存笔记草稿，前端上传图片后调用updateBlogImages()更新图片地址，此时触发pushBloToFansBatch()将笔记ID通过Redis Pipeline批量推送到粉丝的Feed流ZSet中。

点赞功能的核心数据结构为blog:liked:{blogId}（ZSet存储已点赞用户ID和时间戳），用户维度使用blog:user_liked:{userId}（Set）做快速去重。点赞时更新数据库liked字段，同时维护Redis中的点赞记录和Top5排行榜。

Feed流查询采用滚动分页：调用rangeByScoreWithScores(key, 0, max, offset, count)，利用上一次查询的最小时间戳作为下一次的max值，结合offset参数处理同一时间戳多条数据的偏移问题。"

### 修改 7：3.5 功能测试 → 补充完整
"经测试，各模块功能运行正常：
（1）双令牌鉴权：登录认证成功签发双令牌，Access Token过期后前端自动刷新，登出后令牌失效。
（2）多级缓存：热点商铺查询命中Redis缓存，逻辑过期后由异步线程重建，缓存穿透时空值缓存生效。
（3）GEO位置服务：按商铺类型搜索附近商户，返回距离信息和商户详情，支持按评分、销量排序。
（4）社交互动：笔记发布成功推送到粉丝Feed流，点赞/取消点赞实时更新，点赞排行榜Top5展示正常。"

### 修改 8：附录 4. 社交互动核心代码 → 补充完整
当前只显示了likeBlog()方法头，需要补充完整的代码内容：
`
public Result likeBlog(Long id) {
    Long userId = UserHolder.getUser().getId();
    String key = "blog:liked:" + id;
    Double score = stringRedisTemplate.opsForZSet().score(key, userId.toString());
    if (score == null) {
        boolean update = update().setSql("liked = liked + 1").eq("id", id).update();
        if (update) {
            stringRedisTemplate.opsForZSet().add(key, userId.toString(), System.currentTimeMillis());
        }
    } else {
        boolean update = update().setSql("liked = liked - 1").eq("id", id).update();
        if (update) {
            stringRedisTemplate.opsForZSet().remove(key, userId.toString());
        }
    }
    return Result.ok();
}
`

以及pushBloToFansBatch和queryBlogOfFollow的完整方法。

---

## 操作步骤

1. 你确认上述第1-8项修改内容
2. 我执行 python-docx 脚本：
   - 不改动封面/评分表/任务书的结构
   - 只修改指定文本段
   - 在占位符处插入 PNG 图片，图片宽度适配页面
   - 代码字体设置为 Times New Roman
   - 页眉修改为"基于Spring Boot的小众点评系统开发"
3. 生成新的 docx 文件（在原文件名基础上操作）
